# Pre-generated apr-util configure headers

See `third_party/apr/gen/README.md` for the full layout, arch-selection wiring,
and the regeneration recipe — the apr-util headers here are produced in the same
`./configure` run as the APR headers and share that documentation.
