Makefile
!third_party/libjpeg_turbo/yasm/source/config/linux/Makefile
*.mk
!install/**/*.mk
*.Makefile
client.timestamp
hooks.timestamp
build/gyp_helper.pyc
build/landmine_utils.pyc
out/
tools/closure/
!devel/Makefile
release/
log/
bazel-*
.vscode/ipch
.vscode/.vs_code_bazel_build/vs_code_aspect.bzl
external/
compile_commands.json
clang.bazelrc
.vscode/*.log
build/lastchange.sh
.claude/settings.local.json

# Development scripts
fix-ssh.ps1
sshfix.bat
llvm.sh

# Temp files
nul
*.log
!.vscode/*.log

# Local Docker Compose overrides
docker-compose.override.yml

# Local Bazel overrides (machine-specific paths and performance tuning)
user.bazelrc

# Vendored dependencies (generated by tools/vendor-deps.sh, shipped in source tarballs)
vendor/

# Admin console SPA build artifacts
pagespeed/system/console/node_modules/
pagespeed/system/console/dist/

# Report-only dependency scan output (tools/sbom/dep-scan.sh — the design record)
sbom/scan/

# Vendored-C/C++ CVE matcher cache + reports
tools/dependency/.cve-cache/

# Python bytecode (e.g. from tools/dependency importing tools/generate-sbom.py)
__pycache__/
*.pyc

# the design record in-container nginx module build scratch tree (build_module_in_container.sh)
.ngx-module-build/
test/browser/node_modules/

# parity adapter-arm container build (HOME=/workspace) droppings
/.cache/
/.ssh/

# Apache RAT report written by tools/ci/rat.sh
rat-report.txt
