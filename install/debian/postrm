#!/bin/sh
# Cache and log directories (/var/cache/mod_pagespeed, /var/log/pagespeed)
# are preserved on purpose (standard Unix convention).
# See install documentation for cleanup instructions.
exit 0
